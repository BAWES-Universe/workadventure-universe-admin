# Development Dockerfile for Admin API
# Runs behind Traefik at admin.workadventure.localhost

FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm install

# Copy source code (will be overridden by volume mount, but needed for initial build)
COPY . .

# Expose port (used by Traefik)
EXPOSE 3333

# Start development server
# Next.js dev server binds to 0.0.0.0 by default in Docker
CMD ["npm", "run", "dev"]

